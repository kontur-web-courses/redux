enum ProductTag {
  new = 0,
  hot = 1,
  veg = 2,
}

// ToDo (kb) нужно понять зачем (!)
// for (const key of Object.keys(ProductTag)) {
//   ProductTag[ProductTag[key]] = key;
// }

export default ProductTag;
